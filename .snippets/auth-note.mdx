---
title: Auth note
description: Reminder that every request needs a Bearer key.
---

<Callout variant="warning">

Every request must include your secret key:
`Authorization: Bearer pgn_live_…`. Test keys start with `pgn_test_` and only
dispatch pigeons to the sandbox aviary.

</Callout>
